# genai-readme.md — Техническое задание для AI-агента Cursor

**Это системный промпт и техническое ТЗ для AI-агента в Cursor.**

Задача агента: **полностью создать и довести до рабочего состояния проект `product-mcp`** — MCP-сервер + Telegram-бот для каталога цифровых игр.

---

## 📋 Общая концепция

### Архитектура системы

```
Пользователь (Telegram)
       ↓
Telegram Bot (aiogram)
       ↓
OpenAI GPT-4 (принятие решений)
       ↓
MCP Server (HTTP API)
       ↓
SQLite Database (products.db)
```

**Предметная область:** Цифровой магазин игр  
**Основная сущность:** Product (игра) с полями: id, name, category, price, platform, is_featured

---

## 🎯 Основные требования

### Приоритеты

1. **Работоспособность:** Код должен запускаться сразу после `pip install -r requirements.txt`
2. **Читаемость:** Чистый код, понятные имена, комментарии на ключевых местах
3. **Минимальные диффы:** Изменения только по задаче, не переписывать рабочий код
4. **Соответствие уроку:** Следовать концепции MCP из материалов курса

---

## 🗄️ База данных

### Схема таблицы `products`

```sql
CREATE TABLE IF NOT EXISTS products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    category TEXT NOT NULL,
    price REAL NOT NULL,
    platform TEXT NOT NULL,
    is_featured INTEGER NOT NULL DEFAULT 0
);
```

### Категории игр (минимум)

- Action
- RPG
- Strategy
- Indie
- Adventure
- Shooter
- Sports
- Simulation

### Платформы

- PC
- PlayStation
- Xbox
- Switch
- Mobile

### Инициализация данных

**Требования к тестовым данным:**
- Минимум 100 игр
- Все категории представлены (минимум 8 игр в каждой)
- Все платформы представлены
- Цены: от 5 до 70 единиц
- 10-15 игр должны иметь `is_featured = 1`

**Примеры игр для инициализации:**
- The Witcher 3, RPG, 40, PC, featured
- Hollow Knight, Indie, 15, PC
- Elden Ring, RPG, 60, PlayStation
- Cyberpunk 2077, Action, 60, PC
- Stardew Valley, Simulation, 15, Switch
- и т.д.

---

## 🔧 MCP-сервер (`mcp_server/`)

### Стек технологий

- **Web framework:** FastAPI (предпочтительно)
- **ASGI server:** Uvicorn
- **Database:** SQLite3 (стандартная библиотека)
- **Validation:** Pydantic (встроена в FastAPI)

### Структура файлов

```
mcp_server/
├── server.py         # FastAPI app, эндпоинты
├── db.py            # БД логика, CRUD операции
├── tools.py         # MCP-инструменты (описания + маппинг)
├── requirements.txt # Зависимости
└── products.db      # SQLite БД (создается автоматически)
```

### server.py — Эндпоинты

**1. GET `/tools`**
```python
# Возвращает список всех MCP-инструментов
Response: List[Dict]  # MCP_TOOLS из tools.py
```

**2. POST `/call_tool`**
```python
# Вызывает конкретный MCP-инструмент
Request: {
    "tool": str,          # имя инструмента
    "arguments": dict     # аргументы инструмента
}

Response (успех): {
    "ok": true,
    "result": any         # результат выполнения
}

Response (ошибка): {
    "ok": false,
    "error": str          # описание ошибки
}
```

### db.py — Функции для работы с БД

**Обязательные функции:**

```python
def init_db() -> None:
    """Создаёт таблицу и заполняет тестовыми данными"""
    
def get_all_products() -> List[Dict]:
    """Возвращает все игры"""
    
def find_products_by_name(name: str) -> List[Dict]:
    """Поиск по частичному совпадению имени (LIKE)"""
    
def find_products_by_category(category: str) -> List[Dict]:
    """Поиск по жанру"""
    
def find_products_by_platform(platform: str) -> List[Dict]:
    """Поиск по платформе"""
    
def find_products_by_price_range(min_price: float, max_price: float) -> List[Dict]:
    """Поиск в диапазоне цен"""
    
def add_product(name: str, category: str, price: float, 
                platform: str, is_featured: int = 0) -> Dict:
    """Добавляет новую игру, возвращает созданный объект"""
    
def get_featured_products() -> List[Dict]:
    """Возвращает рекомендованные игры (is_featured = 1)"""
    
def find_similar_products(base_name: str) -> List[Dict]:
    """Находит похожие игры по жанру и платформе базовой игры"""
```

**Формат возвращаемых данных:**
```python
{
    "id": int,
    "name": str,
    "category": str,
    "price": float,
    "platform": str,
    "is_featured": int
}
```

### tools.py — MCP-инструменты

**Структура описания инструмента:**

```python
MCP_TOOLS = [
    {
        "name": "имя_инструмента",
        "description": "Подробное описание на русском",
        "inputSchema": {
            "type": "object",
            "properties": {
                "param_name": {
                    "type": "string|number|...",
                    "description": "описание параметра"
                }
            },
            "required": ["param_name"]  # если обязательные
        }
    }
]
```

**Обязательные инструменты:**

1. **list_products**
   - Описание: "Возвращает список всех игр в каталоге"
   - Аргументы: нет

2. **find_product**
   - Описание: "Ищет игры по частичному совпадению названия"
   - Аргументы: `name` (string)

3. **find_products_by_category**
   - Описание: "Находит игры определённого жанра"
   - Аргументы: `category` (string)

4. **find_products_by_platform**
   - Описание: "Находит игры для конкретной платформы"
   - Аргументы: `platform` (string)

5. **find_products_by_price_range**
   - Описание: "Находит игры в заданном ценовом диапазоне"
   - Аргументы: `min_price` (number), `max_price` (number)

6. **add_product**
   - Описание: "Добавляет новую игру в каталог"
   - Аргументы: `name`, `category`, `price`, `platform`, `is_featured` (опционально)

7. **list_featured_products**
   - Описание: "Возвращает список рекомендованных игр"
   - Аргументы: нет

8. **find_similar_products**
   - Описание: "Находит похожие игры по жанру и платформе"
   - Аргументы: `base_name` (string)

9. **calculate**
   - Описание: "Безопасно вычисляет математическое выражение"
   - Аргументы: `expression` (string)
   - **ВАЖНО:** Не использовать `eval()`! Использовать модуль `ast` или библиотеку `simpleeval`

**Маппинг инструментов на функции:**

```python
TOOL_FUNCTIONS = {
    "list_products": get_all_products,
    "find_product": find_products_by_name,
    # ... и т.д.
}
```

### requirements.txt для MCP-сервера

```
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0
simpleeval>=0.9.13
```

---

## 🤖 Telegram-бот (`telegram_bot/`)

### Стек технологий

- **Bot framework:** aiogram 3.x (предпочтительно)
- **LLM API:** OpenAI Python SDK
- **HTTP client:** httpx или requests
- **Config:** python-dotenv

### Структура файлов

```
telegram_bot/
├── bot.py           # Основная логика бота, хендлеры
├── config.py        # Конфигурация, чтение .env
├── mcp_client.py    # HTTP-клиент для MCP-сервера
└── requirements.txt # Зависимости
```

### config.py

```python
import os
from dotenv import load_dotenv

load_dotenv()

OPENAI_API_KEY = os.getenv("OPENAI_API_KEY")
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")
MCP_SERVER_URL = os.getenv("MCP_SERVER_URL", "http://localhost:8000")
OPENAI_MODEL = os.getenv("OPENAI_MODEL", "gpt-4o-mini")
```

### mcp_client.py

**Функции:**

```python
def get_tools() -> List[Dict]:
    """Получает список MCP-инструментов с сервера"""
    # GET {MCP_SERVER_URL}/tools
    
def call_tool(tool_name: str, arguments: dict) -> dict:
    """Вызывает MCP-инструмент"""
    # POST {MCP_SERVER_URL}/call_tool
    # Возвращает {"ok": bool, "result": any, "error": str}
```

**Обработка ошибок:**
- Таймауты: 10 секунд
- Если сервер недоступен: вернуть `{"ok": False, "error": "MCP server unavailable"}`
- Логировать все ошибки в консоль

### bot.py — Системный промпт

**Важно:** Промпт должен быть на русском языке!

```python
SYSTEM_PROMPT = """
Ты — умный помощник по каталогу цифровых игр. 

Доступные инструменты:
1. list_products — показать все игры
2. find_product — найти игру по названию
3. find_products_by_category — найти игры жанра
4. find_products_by_platform — найти игры для платформы
5. find_products_by_price_range — найти игры в ценовом диапазоне
6. add_product — добавить новую игру
7. list_featured_products — показать рекомендованные игры
8. find_similar_products — найти похожие игры
9. calculate — выполнить математическое вычисление

Формат вызова инструмента (JSON):
{
  "tool": "название_инструмента",
  "arguments": {"параметр": "значение"}
}

Примеры запросов пользователя и ответов:

Запрос: "покажи все игры"
Ответ: {"tool": "list_products", "arguments": {}}

Запрос: "найди игру ведьмак"
Ответ: {"tool": "find_product", "arguments": {"name": "ведьмак"}}

Запрос: "покажи RPG игры"
Ответ: {"tool": "find_products_by_category", "arguments": {"category": "RPG"}}

Запрос: "игры на PC"
Ответ: {"tool": "find_products_by_platform", "arguments": {"platform": "PC"}}

Запрос: "игры дешевле 30"
Ответ: {"tool": "find_products_by_price_range", "arguments": {"min_price": 0, "max_price": 30}}

Запрос: "добавь игру Hades цена 25 жанр Action платформа PC"
Ответ: {"tool": "add_product", "arguments": {"name": "Hades", "category": "Action", "price": 25, "platform": "PC"}}

Запрос: "покажи похожие на Witcher 3"
Ответ: {"tool": "find_similar_products", "arguments": {"base_name": "Witcher 3"}}

Запрос: "сколько будет 123 * 456"
Ответ: {"tool": "calculate", "arguments": {"expression": "123 * 456"}}

Если инструмент не нужен, отвечай обычным текстом.
Отвечай дружелюбно, на русском языке.
Если запрос непонятен, задавай уточняющие вопросы.
"""
```

### bot.py — Логика обработки сообщений

**Алгоритм:**

1. Получить сообщение от пользователя
2. Добавить в историю диалога (сохранять контекст)
3. Отправить запрос к OpenAI:
   - Системный промпт
   - История диалога (последние 10-20 сообщений)
4. Получить ответ от LLM
5. Проверить: это JSON с полем "tool" или обычный текст?
6. Если JSON:
   - Извлечь `tool` и `arguments`
   - Вызвать `mcp_client.call_tool(tool, arguments)`
   - Добавить результат в контекст
   - Попросить LLM сформулировать финальный ответ
7. Отправить ответ пользователю (красиво отформатированный)

**Контекст диалога:**

```python
# Структура для хранения истории
user_contexts = {}  # user_id -> List[message_dict]

def get_context(user_id: int) -> List[dict]:
    if user_id not in user_contexts:
        user_contexts[user_id] = []
    return user_contexts[user_id]

def add_to_context(user_id: int, role: str, content: str):
    context = get_context(user_id)
    context.append({"role": role, "content": content})
    # Ограничение: максимум 20 последних сообщений
    if len(context) > 20:
        context.pop(0)
```

**Форматирование ответов:**

Для списков игр использовать формат:
```
🎮 Название игры
   Платформа: PC
   Жанр: RPG
   Цена: 40 ₽
   
🎮 Другая игра
   ...
```

### requirements.txt для Telegram-бота

```
aiogram>=3.0.0
openai>=1.0.0
httpx>=0.25.0
python-dotenv>=1.0.0
```

---

## 📝 Файлы конфигурации

### .env.example

```env
# OpenAI API Key
OPENAI_API_KEY=sk-proj-your-key-here

# Telegram Bot Token from @BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# MCP Server URL
MCP_SERVER_URL=http://localhost:8000

# OpenAI Model (optional)
OPENAI_MODEL=gpt-4o-mini
```

### .gitignore

```
# Environment
.env
.venv/
venv/
__pycache__/
*.pyc

# Database
*.db
*.sqlite

# IDE
.vscode/
.idea/
.cursor/

# OS
.DS_Store
Thumbs.db
```

---

## 🧪 Тестовые сценарии

**Перед завершением работы убедиться, что работают следующие сценарии:**

### Сценарий 1: Список игр
```
Пользователь: покажи все игры
Бот: [список игр из БД]
```

### Сценарий 2: Поиск по имени
```
Пользователь: найди игру witcher
Бот: [игры с "witcher" в названии]
```

### Сценарий 3: Поиск по жанру
```
Пользователь: покажи игры жанра RPG
Бот: [игры категории RPG]
```

### Сценарий 4: Поиск по платформе
```
Пользователь: какие игры есть на PlayStation
Бот: [игры для PlayStation]
```

### Сценарий 5: Поиск по цене
```
Пользователь: покажи игры дешевле 30 долларов
Бот: [игры с price < 30]
```

### Сценарий 6: Добавление игры
```
Пользователь: добавь игру Hollow Knight цена 15 жанр Indie платформа PC
Бот: Игра добавлена! [детали]

Пользователь: найди hollow knight
Бот: [показывает добавленную игру]
```

### Сценарий 7: Похожие игры
```
Пользователь: найди игру witcher 3
Бот: [показывает Witcher 3]

Пользователь: покажи похожие игры
Бот: [игры того же жанра и/или платформы]
```

### Сценарий 8: Рекомендованные игры
```
Пользователь: что порекомендуешь?
Бот: [игры с is_featured = 1]
```

### Сценарий 9: Калькулятор
```
Пользователь: сколько будет 123 * 456
Бот: 123 * 456 = 56088
```

### Сценарий 10: Контекст диалога
```
Пользователь: найди игру hollow knight
Бот: [показывает Hollow Knight]

Пользователь: а сколько она стоит?
Бот: Hollow Knight стоит 15 ₽ [из контекста помнит о какой игре речь]
```

---

## ✅ Чек-лист готовности проекта

Перед завершением работы проверить:

**MCP-сервер:**
- [ ] Запускается командой `python server.py`
- [ ] Создаёт `products.db` с 100+ играми
- [ ] Эндпоинт `/tools` возвращает список инструментов
- [ ] Эндпоинт `/call_tool` корректно вызывает все 9 инструментов
- [ ] Ошибки обрабатываются и логируются

**Telegram-бот:**
- [ ] Запускается командой `python bot.py`
- [ ] Отвечает на команду `/start`
- [ ] Правильно парсит ответы от OpenAI
- [ ] Вызывает MCP-инструменты через HTTP
- [ ] Сохраняет контекст диалога
- [ ] Красиво форматирует ответы

**Общее:**
- [ ] Все 10 тестовых сценариев работают
- [ ] Нет критических ошибок в консоли
- [ ] Код читаемый, с комментариями
- [ ] `README.md` актуален и информативен
- [ ] `.env.example` содержит все нужные переменные

---

## 🎯 Приоритеты при изменениях

1. **Работоспособность >** Красота кода
2. **Минимальные диффы >** Полный рефакторинг
3. **Соответствие ТЗ >** Творческие улучшения
4. **Тестирование >** Предположения

**Золотое правило:** Не ломай то, что уже работает!

---

## 📚 Справочная информация

### Структура MCP-инструмента (JSON Schema)

```json
{
  "name": "tool_name",
  "description": "Что делает инструмент",
  "inputSchema": {
    "type": "object",
    "properties": {
      "param1": {
        "type": "string",
        "description": "Описание параметра"
      }
    },
    "required": ["param1"]
  }
}
```

### Типы параметров JSON Schema

- `string` — текстовые данные
- `number` — числа (int или float)
- `integer` — только целые числа
- `boolean` — true/false
- `array` — массив
- `object` — объект

### Безопасный калькулятор (пример)

```python
from simpleeval import simple_eval

def calculate(expression: str) -> float:
    """Безопасно вычисляет математическое выражение"""
    try:
        result = simple_eval(expression)
        return result
    except Exception as e:
        raise ValueError(f"Ошибка вычисления: {e}")
```

---

**Это ТЗ — твой источник истины. Следуй ему при создании проекта.**
